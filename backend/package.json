{
    "name": "app-backend",
    "version": "1.0.0",
    "description": "node.js backend on docker",
    "main": "server.js",
    "dependencies": {
        "express": "^4.17.1",
        "nodemon": "^2.0.6",
        "pg": "^8.4.2",
        "sequelize": "^6.3.5"
    },
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon -e ts --exec ”tsc -p ./tsconfig.json && node ./src/server.js"
    },
    "devDependencies": {
        "typescript": "^4.0.3"
    }
}
